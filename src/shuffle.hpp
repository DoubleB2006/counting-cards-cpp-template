#ifndef SHUFFLE_HPP
#define SHUFFLE_HPP

#include <vector>

// Three shuffle algorithms
void shuffle1(std::vector<int>& deck);
void shuffle2(std::vector<int>& deck);
void shuffle3(std::vector<int>& deck);

// Helper
void printDeck(const std::vector<int>& deck);

#endif
